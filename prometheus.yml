global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "spring-app"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["spring-app:8081"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]